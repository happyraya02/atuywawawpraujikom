{{-- @extends('layouts.index')
@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">Menampilkan Data Tag</div>
                <div class="card-body">
    <div class="form-group">
        <label for="">Tag</label>
        <input class="form-control" value="{{ $tag->nama }}" type="text" name="nama" disabled>
    </div>
    <div class="form-group">
        <a href="{{ url('tag') }}" class="btn btn-outline-info">Kembali</a>
    </div>
        </div>
            </div>
                </div>
                    </div>
                        </div>
                            @endsection --}}
